import React from "react";

const Title = ({ text }) => {
  return (
    <div className="text-custom-white font-anticDidone font-thin text-5xl translate-x-6">
      {text}
    </div>
  );
};

export default Title;
